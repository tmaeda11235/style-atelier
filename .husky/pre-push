#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "? ERROR: Direct 'git push' is disabled to prevent heavy CI wait times."
echo ""
echo "Please use 'npm run push' to push your changes."
echo "This command will run Smart Incremental Tests locally, generate a cryptographic"
echo "Proof of Work, and securely bypass the heavy CI test suite."
echo ""
echo "If you REALLY want to bypass local testing and force full CI runs, use:"
echo "git push --no-verify"
echo ""
exit 1

